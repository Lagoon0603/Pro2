Prob37
実行結果：
(1) x = 4
(2) x = 8
(3) x = 8
(4) p = (0, 0)
(5) p = (0, 0)
(6) p = (2, 4)
(7) a[0] = 20
    a[1] = 24
理由：
(1)
xに初期値の4が代入されるため、x=4が出力される。
(2)
change1メソッドで引数に4を足した8をreturnし、それを変数xに代入しているため、x=8が出力される。
(3)
変数xはint型であり、これは基本型である。
change2メソッドには変数xの値がコピーされて渡される。
メソッド内で仮引数xをインクリメントしても、コピーされた値を変更しただけであり、呼び出し元の変数xには一切影響を与えない。
したがって、x=8が出力される。
(4)
MyPointクラスのインスタンスを生成した直後であり、インスタンス変数xとyは値を指定して初期化されていない。
よってデフォルト値の0がx,yともに出力される。
(5)
変数pはMyPointクラスのインスタンスを指す参照型である。
changePoint1に引数として渡されるのは、インスタンスの参照情報のコピーあり、メソッド内で p = new MyPoint(); としているが、これはコピーされた変数pの参照先を新しいインスタンスに向け直しただけであり、呼び出し元の変数pが指している本来のインスタンスには何も影響しない。
したがって、x,yともに0が出力される。
(6)
changePoint2でも同じく参照型として参照情報のコピーが渡されるが、ここでは p.setPoint(2, 4); と呼び出し元と同じインスタンスに対して操作を行っている。
同じ実体を変更しているため、呼び出し元の変数pから見ても中身が(2, 4)に変更されている。よって、p = (2, 4)が出力される。
(7)
配列（int[]）もオブジェクトであり、参照型である。
(6)と同様に、配列の実体を示す参照が渡され、その参照先にある要素（a[0]）の値を直接 20 に書き換えているため、呼び出し元でも変更が反映される。
a[1]には何の操作も加えていないため、24のまま出力される。


Prob38
2点間の距離は16.1245154965971


Prob310
実行結果：
count = 0
0: true
1: false
2: true
3: false
4: true
5: false
6: true
count = 4
Index for 0: 1
Index for 4: 3
0: false
1: false
2: true
3: false
4: true
5: false
6: true
count = 3
Index for 0: -1
Index for 4: 2
説明：
実行前の配列は、追加された順に[2, 0, 6, 4]であり、要素数(num)は4である。
s.remove(0)が実行されると、removeメソッドで0が削除されて、配列は[2, 6, 4]となる。
そのため、s.find(0)は存在しなくなったためfalseとなり、countは4から3に減少する。
getIndex(0)は存在しなくなったため、-1を出力する。
また、削除された0の後ろにあった要素4のインデックスが前へ一つずれたため、getIndex(4)の結果が3から2へと変化した。

